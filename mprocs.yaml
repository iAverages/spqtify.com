procs:
  api:
    cwd: "apps/api"
    shell: "cargo run"
  embed-image-service:
    cwd: "apps/embed-image-service"
    shell: "pnpm run dev"
  embed-image:
    cwd: "packages/embed-image"
    shell: "pnpm run dev"
